/*
 * This class stores all of the global constants used in this application. 
 * These constants are set on the login page.
 */
public class Constants {
	
	public static String userName = ""; //Database login (default is "points")
	public static String password = ""; //Database password (check Officer wiki for password)
	public static String databaseName = ""; //Database name (default is "points")
	public static String tableName = ""; // Table name (default format is points_<semester: fall/string>_<yr: xx>, i.e. points_fall_16)
	
}
